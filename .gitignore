# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc.*

# Virtual environments
.venv/
venv/
env/

# Streamlit
*.stderr.log
*.stdout.log
.streamlit/secrets.toml

# Tool caches / temp
.cache/
*.bak
*.tmp

# pdf_card_editor: user project data (generated locally, not shared)
cardlist_referenced/pdf_card_editor/projects/

# Large PDF files (exceed GitHub 100MB limit)
cardlist_referenced/*.pdf

# Competition-provided data (download from Kaggle)
data/*
!data/README.md

# Local submission bundle
submission.tar.gz
sample_submission/submission.tar.gz
sample_submission/submission*.tar.gz
tmp_submit_check/
.tmp_readme_push/

# Battle Review Viewer: generated replay JSON (keep the folder via .gitkeep)
battle_review_viewer/replays/*.json

# RL training checkpoints (large torch zips); ship only policy.npz
sample_submission/models/
sample_submission/models_rs/
# Experimental policy export (promoted into policy.npz when adopted)
sample_submission/policy_rs.npz
*.tensorboard
runs/

# Claude Code internal settings
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
