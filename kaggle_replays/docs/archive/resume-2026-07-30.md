# 再開手順（2026-07-30 電源断時点）

> **状態: 古い（保管）** — 2026-07-30 の電源断からの復旧手順。当該の実行はすべて終了済みで、
> 記載の Kaggle カーネルはもう走っていない。現状は [../README.md](../README.md)。

## 1. Kaggle は止まっていない

**5本の学習がリモートで走り続けている。** PC の電源とは無関係。

```
koshin953/ptcg-train-alakazam-k60     alakazam ミラー 60iter
koshin953/ptcg-train-lucario-k60      mega_lucario_ex ミラー 60iter
koshin953/ptcg-train-archaludon-k60   archaludon_ex ミラー 60iter
koshin953/ptcg-train-crustle-k60      crustle ミラー 60iter
koshin953/ptcg-train-marnie-k60       marnie_grimmsnarl_ex ミラー 60iter
```

再開したらまず状態確認:

```bash
for k in alakazam lucario archaludon crustle marnie; do
  kaggle kernels status koshin953/ptcg-train-$k-k60
done
```

完了していたら回収する。**回収しないと重みが取れない。**

```bash
kaggle kernels output koshin953/ptcg-train-<name>-k60 -p kaggle_replays/rl/kaggle_out/<name>
```

出力に `policy_weights_<learner>_pool_k60.json` と `_train_pool_*.log` が入っている。
重みは `sample_submission/ptcg_ai/learning/` へ手でコピーする。

---

## 2. 中断したもの

### デッキ切れ報酬の A/B（10/20 iter で中断）

ログは `kaggle_replays/value_net_probe/logs/ab_{nopen,pen05}.log`。
**best チェックポイントは iter 完了時にしか書き出されないので、重みは残っていない。**
やり直す場合:

```bash
python kaggle_replays/rl/train_pool.py --learner alakazam --train-opponents alakazam \
    --eval-fixed alakazam --iters 20 --eval-every 5 --tag ab_nopen
python kaggle_replays/rl/train_pool.py --learner alakazam --train-opponents alakazam \
    --eval-fixed alakazam --iters 20 --eval-every 5 --deckout-penalty 0.5 --tag ab_pen05
```

**中断時点(iter 10)の暫定値**:

| | 学習時勝率 | デッキ切れ率 |
|---|---:|---:|
| 罰なし | 0.422 | 0.395 |
| 罰0.5 | 0.379 | 0.230 |

デッキ切れは狙いどおり下がったが**勝率も下がっている**。事前に決めた判定基準では
「デッキ切れ率は下がるが勝率が下がる → 不採用」。ただし10 iter は途中経過であり、
最終評価（凍結プール1,200試合）を取っていないので**結論ではない**。

### 上位200チームのログ収集（中断）

**リプレイは 427 → 1,163 件（+736）に増えた。** ただし——

**⚠ `episodes_master.jsonl` が更新されていない（4,975行のまま）。**
`fetch_top_episodes.py` はインデックスを最後（`[5/5]`）にまとめて書く実装なので、
中断すると新しい736件に順位情報が付かない。

**復元は可能。** リプレイの `info.TeamNames` と、保存済みのリーダーボード
スナップショット `index/leaderboard_history/leaderboard-20260730T094742Z.json`
（上位200チーム）を**チーム名で結合**すれば順位が付く。
`_common.py` の `build_master_rows` / `name_to_context` が同じことをしている。

**これをやらないと、増やした736件を「上位だけ選ぶ」目的に使えない。** 再開時の最優先事項。

収集を続けるなら:

```bash
python kaggle_replays/fetch_top_episodes.py --competition pokemon-tcg-ai-battle \
    --top 200 --submissions-per-team 1 --max-episodes 4000
```

既に取得済みのものは再ダウンロードしない（`episode_id` で重複排除）。
実測レート 14.6件/分、1件あたり 4.4MB。

---

## 3. 今日確立したこと（詳細は各 README / 引き継ぎ文書）

| 事実 | 数値 |
|---|---|
| RL は効く | BC 0.399 → SPEC_alakazam 0.536（プール平均、8,000試合、p<1e-9） |
| **その中身は「KO の取り逃し」の解消** | 逸失率 BC 6.3% → RL後 0.1〜0.6%（区間が完全分離） |
| configC（上位濃縮リウェイト）は再現せず | +1.9pt [-1.5, +5.3]、p=0.265 |
| 順位ハードフィルタは有害 | ≤200位のみ 0.128（−27.1pt）、≤1000位 0.308（−9.1pt）。**量が要る** |
| デッキ切れが残る敗因 | 敗北の23〜33%。RL後のモデルほど相手より多い（SPEC_marnie 32.7% 対 8.6%） |
| デッキ切れ判定は閾値0で完全一致 | 400試合220敗で 真陽性60/偽陽性0/偽陰性0 |
| 振り分け（デッキ推定でモデル切替）は効かない | +1.8pt、p=0.30 |
| 相手プール学習 < 固定相手学習 | 0.504 対 0.534 |

---

## 4. 次にやること（優先度順）

1. **Kaggle 5本の回収**（完了していれば）
2. **インデックスの復元**（上記 §2。これをやらないと収集分が死ぬ）
3. **A/B のやり直しと最終評価**（デッキ切れ罰の採否を決める）
4. **段階II**: 鍛え上がった5体でプールを組み直し、プール学習 vs 固定相手を再測定
5. **段階III**: 相互鍛錬ループ（過去世代のチェックポイントを混ぜる）

---

## 5. ブランチ

```
feature/multi-archetype-imitation  ← RL 一式。今日の作業のほとんど
feature/top-replay-utilization     ← 測定・解析（sklearn/numpy のみ）
```

両方とも upstream 無し（push されない）。統合方針は未決。
