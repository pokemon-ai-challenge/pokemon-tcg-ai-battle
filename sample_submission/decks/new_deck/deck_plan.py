
"""デッキ方針（⑥）— フーディン ハンドパワーデッキ。

このファイルはカードIDやカード名を直接扱う「デッキ固有の知識」（担当A領域）。
判断ロジック（decision/ 配下）はここを読むだけで、カード名をコードに直書きしない。
盤面状態（Observation/State）は扱わない。条件付きの使用判定ロジックそのものは
③ メイン行動の意思決定（decision/main_turn_parts/ 配下）の役目であり、
ここに書くのはその実装時に参照される「方針・優先順位・メモ」に限る。

デッキの勝ち筋:
    ケーシィ→ユンゲラー→フーディンで進化し、フーディンの「ハンドパワー」
    （コスト超1個・ダメージ=手札枚数×2）を主砲にする。ノココッチの特性
    「にげあしドロー」やサポート／グッズの大量ドローで手札を積んでから撃つ。
    基本方針は「できるだけ手札を多くしてフーディンで攻撃する」。
"""

from __future__ import annotations

from dataclasses import dataclass

from ptcg_ai.shared.profile_types import (
    EnergyCardContext,
    EnergyPriorityRule,
    MatchupPlan,
    SearchPriorityRule,
    UsageContext,
)


@dataclass(frozen=True)
class AttackerPlan:
    card_id: int
    name: str
    note: str


@dataclass(frozen=True)
class PriorityEntry:
    card_id: int
    name: str
    reason: str


@dataclass(frozen=True)
class PrizeStageWinCondition:
    prize_range: str
    plan: str


@dataclass(frozen=True)
class UsageNote:
    """グッズ／サポート／スタジアムの使用方針メモ。

    盤面状態を判定するロジックそのものではなく、③実装時に参照される
    「どういう場面で使うべきか」の言語化メモ。
    """

    card_id: int
    name: str
    note: str


# ---------------------------------------------------------------------------
# フーディン系列（ケーシィ→ユンゲラー→フーディン）
# ---------------------------------------------------------------------------

FUDIN_LINE_CARD_IDS: list[int] = [741, 742, 743]  # ケーシィ / ユンゲラー / フーディン

# ノコッチ系列（ノコッチ→ノココッチ）。エネルギー周回コンボ（下記 ENERGY_RECYCLE_TARGET_CARD_IDS）で
# リッチエネルギーの一時置き場として使う対象。進化前のノコッチに付けたエネルギーも進化で
# ノココッチにそのまま引き継がれるため、周回の受け皿はどちらの段階でも構わない。
DUNSPARCE_LINE_CARD_IDS: list[int] = [65, 66]  # ノコッチ / ノココッチ


# ---------------------------------------------------------------------------
# 主力・サブアタッカー
# ---------------------------------------------------------------------------

MAIN_ATTACKER = AttackerPlan(
    card_id=743,
    name="フーディン",
    note=(
        "ハンドパワー（超1個）でダメージ=手札枚数×2。"
        "ドローを伸ばしてから撃つのが本デッキの主な勝ち筋。"
    ),
)

# キチキギスex（旧サブアタッカー）はデッキから抜けたため現在は空。
# ノココッチはランドクラッシュ（●●●,90dmg）を持つが、下記 BENCH_ONLY_SUPPORT の通り
# 基本的にバトル場に出さない運用のためサブアタッカー扱いにしない。
SUB_ATTACKERS: list[AttackerPlan] = []


# ---------------------------------------------------------------------------
# ベンチに留めておきたいポケモン（自分から場に出さない）
# ---------------------------------------------------------------------------
#
# シェイミ・ノココッチはどちらも特性が主な役割で、攻撃力・HPも高くない。
# バトル場に出す（にげる・入れ替え・進化・きぜつ後の後継など）とワザで狩られやすく、
# 特性の継続利用や被弾保護の恩恵を失うため、基本的にベンチから動かさない。
# 撤退先・入れ替え先の候補としても優先度を下げる。
# ベンチのうち2枠は確実にノコッチ／ノココッチ用に確保する。

RESERVED_BENCH_SLOTS_FOR_DRAW_ENGINE = 2

# ↑の予約枠を「占有するとみなす」ライン（ノコッチ／ノココッチ）と、枠が空いているときに
# 優先展開するたね（ノコッチ）。にげあしドローは使うとノコッチ＋ノココッチが山札に戻るため、
# 毎ターン穴なく回すには「次ターン進化させる予備ノコッチ」を常にベンチへ確保し続ける必要がある。
# 盤面のノコッチ／ノココッチ数が RESERVED_BENCH_SLOTS_FOR_DRAW_ENGINE 未満なら、
# ノコッチのベンチ展開を最優先にする（priorities/board.py が参照）。
RESERVED_BENCH_LINE_IDS = frozenset({65, 66})  # ノコッチ / ノココッチ
RESERVED_BENCH_BASIC_ID = 65  # ノコッチ（枠が空いていれば優先展開するたね）

BENCH_ONLY_SUPPORT: list[PriorityEntry] = [
    PriorityEntry(
        card_id=343,
        name="シェイミ",
        reason=(
            "特性はなのカーテンでベンチ全体をワザダメージから守るのが役割。"
            "バトル場に出すと自身が狩られやすく、守る対象を失う本末転倒になるため"
            "基本的にベンチに固定する。"
        ),
    ),
    PriorityEntry(
        card_id=66,
        name="ノココッチ",
        reason=(
            "特性にげあしドロー（毎ターン3ドロー→自身は山札に戻る）を回すのが役割。"
            "ランドクラッシュでの攻撃運用は基本的に取らず、バトル場には出さない。"
        ),
    ),
]


# ---------------------------------------------------------------------------
# 初手・ベンチ展開で優先したいポケモンの順位
# ---------------------------------------------------------------------------
#
# 上から優先度が高い。

OPENING_BENCH_PRIORITY: list[PriorityEntry] = [
    PriorityEntry(
        card_id=741,
        name="ケーシィ",
        reason=(
            "フーディン系列の起点。進化過程のドロー（サイコドロー）とメイン攻撃を"
            "兼ねる本デッキの中核ライン。最優先。"
        ),
    ),
    PriorityEntry(
        card_id=742,
        name="ユンゲラー",
        reason="フーディン系列。ケーシィと同格の最優先ライン。",
    ),
    PriorityEntry(
        card_id=743,
        name="フーディン",
        reason="フーディン系列。ケーシィと同格の最優先ライン。",
    ),
    PriorityEntry(
        card_id=65,
        name="ノコッチ",
        reason="特性にげあしドロー（ノココッチ）の起点。ベンチに確保する（BENCH_ONLY_SUPPORT参照）。",
    ),
    PriorityEntry(
        card_id=66,
        name="ノココッチ",
        reason="特性にげあしドローの実行役。ベンチに確保する（BENCH_ONLY_SUPPORT参照）。",
    ),
    PriorityEntry(
        card_id=343,
        name="シェイミ",
        reason="特性はなのカーテンによるベンチ保護要員。ベンチに確保する（BENCH_ONLY_SUPPORT参照）。",
    ),
]


# ---------------------------------------------------------------------------
# 進化の優先順位
# ---------------------------------------------------------------------------

EVOLUTION_PRIORITY: list[PriorityEntry] = [
    PriorityEntry(
        card_id=743,
        name="フーディン",
        reason=(
            "ケーシィ→（ふしぎなアメで）フーディン、"
            "または ケーシィ→ユンゲラー→フーディンを最優先で通す。"
            "ユンゲラー／フーディンは手札から進化させるとサイコドローで2〜3枚引けるため、"
            "アメより手動進化の方が噛み合う場面もある。"
        ),
    ),
    PriorityEntry(
        card_id=66,
        name="ノココッチ",
        reason="にげあしドロー起動のため。フーディンルートの余力があれば優先して進化させる。",
    ),
]

# ふしぎなアメ(1079)は基本的にフーディンルート用に温存する。
# ノココッチへの使用は、フーディンルートに使うあてがない余剰分がある場合のみ。
# 使用条件（ITEM_USAGE_NOTES 参照）: バトル場がケーシィ、かつ手札にユンゲラーがなく
# フーディンがある場合に使用する。
RARE_CANDY_CARD_ID = 1079
RARE_CANDY_PRIMARY_TARGET_CARD_ID = MAIN_ATTACKER.card_id


# ---------------------------------------------------------------------------
# エネルギーの優先順位
# ---------------------------------------------------------------------------
#
# 「どのポケモンに付けるか」（ENERGY_TARGET_PRIORITY／ENERGY_REQUIRED_COUNT）と
# 「どのエネルギーカードを使うか」（ENERGY_CARD_PRIORITY_RULES）は別軸。

# フーディン系列（ケーシィ→ユンゲラー→フーディン）は進化段階を問わず最優先。
# 途中の段階（ケーシィ／ユンゲラー）に付けておけば、進化後にそのまま使い回せる。
ENERGY_TARGET_PRIORITY: list[PriorityEntry] = [
    PriorityEntry(
        card_id=743,
        name="フーディン",
        reason=(
            "ハンドパワーは超エネルギー1個で撃てるため最優先。"
            "系列内（ケーシィ／ユンゲラーの段階も含む）はどれも同格の最優先枠として扱う。"
        ),
    ),
    PriorityEntry(
        card_id=742,
        name="ユンゲラー",
        reason="フーディン系列。進化前でもエネルギーを付けて構わない（フーディン743と同格）。",
    ),
    PriorityEntry(
        card_id=741,
        name="ケーシィ",
        reason="フーディン系列。進化前でもエネルギーを付けて構わない（フーディン743と同格）。",
    ),
]
# 上記以外（ノコッチ／ノココッチ、シェイミなど）には基本的にエネルギーを付けない。
# ノココッチはバトル場に出さない運用（BENCH_ONLY_SUPPORT参照）のため、
# 攻撃用のエネルギー付与対象には含めない。
ENERGY_TARGET_CARD_IDS: list[int] = list(FUDIN_LINE_CARD_IDS)

# 各ポケモンの攻撃に必要なエネルギー総数。これ以上は付けない。
ENERGY_REQUIRED_COUNT: dict[int, int] = {
    741: 1,  # ケーシィ
    742: 1,  # ユンゲラー
    743: 1,  # フーディン
    65: 2,  # ノコッチ
    66: 3,  # ノココッチ
    343: 2,  # シェイミ
}

# エネルギーカードの種別:
# - 基本【超】エネルギー(5) / テレパス【超】エネルギー(19) は超エネルギーとして機能する。
# - リッチエネルギー(13, ACE SPEC・1枚のみ)は無エネルギー1個ぶんとして機能する汎用枠。
#   フーディンのハンドパワーはコストが【超】1個指定なので、無エネルギーのリッチ
#   エネルギーだけでは支払えない点に注意（他の超エネルギーが必要）。
BASIC_PSYCHIC_ENERGY_CARD_ID = 5
TELEPATH_ENERGY_CARD_ID = 19
RICH_ENERGY_CARD_ID = 13
ENERGY_RECYCLE_TOOL_CARD_ID = 1146  # ワンダーパッチ：トラッシュの基本超エネルギーをベンチの超ポケモンに再利用

# 相手が使う「ワザの効果を無効化する特殊エネルギー」。改造ハンマー(1081)で優先的に破壊したい対象。
# ミストエネルギー(11): 付いているポケモンは相手のワザの効果を受けない（ダメージは効果ではないので防がない）。
# → こちらの効果付きワザ（キチキギスexのクルーエルアロー＝ベンチ狙撃など）を通したい局面で壊す価値が高い。
# 将来同種のカードが増えたらこの集合に追加する。
OPPONENT_EFFECT_LOCK_ENERGY_IDS: frozenset[int] = frozenset({11})

def _fudin_line_needs_first_energy(ctx: EnergyCardContext) -> bool:
    """付与先がフーディン系列（ケーシィ／ユンゲラー／フーディン）で、まだエネルギーが0個の場合。

    フーディン系列は ENERGY_REQUIRED_COUNT がいずれも1のため、無エネルギーのリッチ
    エネルギーを最初に付けてしまうと「必要数を満たした」扱いになり、以後エネルギーが
    付かなくなる（ハンドパワーは【超】1個指定でリッチエネルギーだけでは支払えないため、
    実際には攻撃できないまま止まってしまう）。ケーシィ／ユンゲラーの段階でも同じ問題が
    起きるため、フーディンだけでなく系列全体を対象にする。
    """
    return ctx.target_card_id in FUDIN_LINE_CARD_IDS and ctx.target_energy_count == 0


def _otherwise(ctx: EnergyCardContext) -> bool:
    """それ以外（フーディン以外への付与、またはフーディンに既にエネルギーが付いている場合）。

    常に True を返す、フォールバック用の最終ルール。
    """
    return True


ENERGY_CARD_PRIORITY_RULES: list[EnergyPriorityRule] = [
    EnergyPriorityRule(
        condition=_fudin_line_needs_first_energy,
        order=[TELEPATH_ENERGY_CARD_ID, BASIC_PSYCHIC_ENERGY_CARD_ID],
    ),
    EnergyPriorityRule(
        condition=_otherwise,
        order=[RICH_ENERGY_CARD_ID, TELEPATH_ENERGY_CARD_ID, BASIC_PSYCHIC_ENERGY_CARD_ID],
    ),
]


# ---------------------------------------------------------------------------
# エネルギー周回コンボ（ノココッチ×リッチエネルギー）
# ---------------------------------------------------------------------------
#
# リッチエネルギー(ACE SPEC・1枚)は、ノココッチの特性「にげあしドロー」
# （3ドロー後、自身と付いているカード全てを山札に戻す。特性自体にエネルギーコストは無い）
# で回収し直せる。バトル場の主力に攻撃可能なだけのエネルギーが既に付いている、または
# ワンダーパッチでトラッシュから後から補給できる場合に限り、余っているリッチエネルギーを
# ノコッチ系列に預けて回す（③実装時: energy_eval.py が ENERGY_REQUIRED_COUNT /
# ワンダーパッチの usage_condition と組み合わせて判定する）。
#
# 受け皿は DUNSPARCE_LINE_CARD_IDS（ノコッチ／ノココッチ）の両方を許可する。進化前のノコッチに
# 付けたリッチエネルギーも進化時にノココッチへそのまま引き継がれるため、どちらの段階で
# 受け取っても周回コンボは成立する（超エネルギー等の攻撃用エネルギーは対象外のまま。
# ノコッチ系はどちらの段階でも攻撃しないデッキ方針のため、超エネを付ける価値は無い）。

ENERGY_RECYCLE_TARGET_CARD_IDS = frozenset(DUNSPARCE_LINE_CARD_IDS)  # ノコッチ / ノココッチ
ENERGY_RECYCLE_CARD_ID = RICH_ENERGY_CARD_ID  # リッチエネルギー
ENERGY_RECYCLE_BACKUP_ITEM_ID = ENERGY_RECYCLE_TOOL_CARD_ID  # ワンダーパッチ

# 付与ルール:
# - ENERGY_REQUIRED_COUNT に定めた必要数以上は付けない。
# - 手元に複数の有効な付与先がある場合は、メイン（バトル場）→場（ベンチ）の順で優先する。


# ---------------------------------------------------------------------------
# にげあしドロー（ドロー系特性）の発動可否の歯止め
# ---------------------------------------------------------------------------
#
# 山札がこの枚数以下ならドロー系特性（にげあしドロー等）を温存する。
# 【2026-07-25、デッキ担当判断】毎ターン欠かさず発動する（floor=0）を採用。
# 根拠: にげあしドローは発動のたびにノコッチ+ノココッチ(+付属カード)が山札へ戻るため、
# 山札1枚からでも2枚に補充される（実測: deck 1->2）。RESERVED_BENCH_SLOTS_FOR_DRAW_ENGINE
# による予備ノコッチのベンチ確保とセットで運用する。
# 既知のリスク（ミラー自己対戦・40戦実測）: floor=0はデッキアウト率82.5%
# （floor=14なら40%）。原因はループ維持率が~45%止まりで、稼働不十分なまま毎ターン
# 撃つと安定域(山札2枚)に速く到達し穴が致命傷になるため。ミラーは両者同時消耗するため
# 非対称マッチアップでの真価は測れておらず、これを承知の上でfloor=0を出荷する。
# 保守的な値に戻したい場合は env `PTCG_DRAW_ABILITY_MIN_DECK=14` で上書きできる
# （このデッキ内固定値ではなく "任意の実行時オーバーライド" として扱う）。
DRAW_ABILITY_MIN_DECK = 0
DRAW_ABILITY_MAX_HAND = 999


# ---------------------------------------------------------------------------
# サーチで最初に探すべきカード
# ---------------------------------------------------------------------------

SEARCH_PRIORITY: list[PriorityEntry] = [
    PriorityEntry(
        card_id=1231,
        name="ヒカリ",
        reason=(
            "たね／1進化／2進化を1枚ずつサーチできるため、"
            "ケーシィ・ユンゲラー・フーディンを一気に揃えられる最重要級サポート。"
        ),
    ),
    PriorityEntry(
        card_id=1152,
        name="ポケパッド",
        reason="展開初期にケーシィ・ノコッチなどのたねポケモンを手札に加える。",
    ),
    PriorityEntry(
        card_id=1086,
        name="なかよしポフィン",
        reason="HP70以下のたねポケモンを2枚までベンチに直接出せる、ポケパッドより速い展開札。",
    ),
    PriorityEntry(
        card_id=1225,
        name="トウコ",
        reason=(
            "進化ポケモンとエネルギーを1枚ずつサーチできる。"
            "ユンゲラー／フーディン＋エネルギーの同時確保に使う。"
        ),
    ),
    PriorityEntry(
        card_id=RARE_CANDY_CARD_ID,
        name="ふしぎなアメ",
        reason="フーディンへの進化短縮用。基本的に手札に来たら温存し、進化ルートのために使う。",
    ),
    PriorityEntry(
        card_id=ENERGY_RECYCLE_CARD_ID,
        name="リッチエネルギー",
        reason=(
            "ACE SPEC・1枚のみ。ノココッチの特性で山札に戻した後は、"
            "トウコ等のエネルギーサーチで優先的に回収し直す。"
        ),
    ),
]

# ---------------------------------------------------------------------------
# 条件付きサーチ優先順位（トウコ等の「進化ポケモン1体」選択で使用）
# ---------------------------------------------------------------------------
#
# SEARCH_PRIORITY にはポケモンのcard_idを含めていない（サポート/グッズ/エネルギーの
# 優先順であり、トウコの進化ポケモン選択のような「複数の進化ポケモンから1体選ぶ」
# 場面では使えないため）。ここではその選択に限定した条件付き優先順を定義する。
#
# ノココッチ（進化先）だけを先にサーチしても、進化元のノコッチが場に無ければ
# 進化させられず手札で腐る。そのため、ノコッチが既に場にいる／ポケパッド・
# なかよしポフィンで今すぐ持ってこられる場合に限りノココッチを最優先にし、
# そうでなければユンゲラー系列（進化前でもエネルギーを付けて使い回せる）を優先する。

_DUNSPARCE_ID = 65  # ノコッチ
_DUNSPARCE_STAGE1_ID = 66  # ノココッチ
_POKE_PAD_CARD_ID = 1152  # ポケパッド
_FRIENDSHIP_MUFFIN_CARD_ID = 1086  # なかよしポフィン


def _dunsparce_reachable(ctx: UsageContext) -> bool:
    """ノコッチが既に自分の場にいる、またはポケパッド／なかよしポフィンで今すぐ持ってこれる場合。"""
    if _DUNSPARCE_ID in ctx.own_board_ids or _DUNSPARCE_STAGE1_ID in ctx.own_board_ids:
        return True
    return _POKE_PAD_CARD_ID in ctx.own_hand_ids or _FRIENDSHIP_MUFFIN_CARD_ID in ctx.own_hand_ids


def _search_otherwise(ctx: UsageContext) -> bool:
    return True


SEARCH_PRIORITY_RULES: list[SearchPriorityRule] = [
    SearchPriorityRule(
        condition=_dunsparce_reachable,
        order=[_DUNSPARCE_STAGE1_ID, 742, 743],  # ノココッチ最優先、次点でユンゲラー系列
    ),
    SearchPriorityRule(
        condition=_search_otherwise,
        order=[742, 743, _DUNSPARCE_STAGE1_ID],  # ノコッチ未確保時はユンゲラー系列を優先
    ),
]


# ---------------------------------------------------------------------------
# 捨てたくないカード
# ---------------------------------------------------------------------------
#
# 夜のタンカ(1097)・スイレンのお世話(1184)・せいなるはい(1129)は
# トラッシュの「ポケモン」「基本エネルギー」を回収できるため、
# それらは多少捨てても後で回収できる。回収手段のないグッズ・サポートを優先的に守りたい。

PROTECT_CARDS: list[PriorityEntry] = [
    PriorityEntry(
        card_id=RARE_CANDY_CARD_ID,
        name="ふしぎなアメ",
        reason="グッズは夜のタンカ等のトラッシュ回収対象外。フーディンルートの生命線。",
    ),
    PriorityEntry(
        card_id=1231,
        name="ヒカリ",
        reason="1枚で進化ライン3段を同時サーチできる要のサポート。回収手段がない。",
    ),
    PriorityEntry(
        card_id=1182,
        name="ボスの指令",
        reason="ベンチ狙撃・詰めに必須のサポート。回収手段がない。",
    ),
]


# ---------------------------------------------------------------------------
# グッズ／サポート／スタジアムの使用方針メモ
# ---------------------------------------------------------------------------
#
# 盤面状態を実際に判定するロジックではなく、③ メイン行動の意思決定
# （decision/main_turn_parts/priorities/ 配下）を実装する際に参照される
# 「どんな場面で使うべきか」の言語化メモ。

ITEM_USAGE_NOTES: list[UsageNote] = [
    UsageNote(
        card_id=RARE_CANDY_CARD_ID,
        name="ふしぎなアメ",
        note="バトル場がケーシィ、かつ手札にユンゲラーがなくフーディンがある場合に使用する。",
    ),
    UsageNote(
        card_id=1081,
        name="改造ハンマー",
        note="相手のバトル場のポケモンに特殊エネルギーが付いている場合に使用する。",
    ),
    UsageNote(
        card_id=1086,
        name="なかよしポフィン",
        note=(
            "ケーシィ・ノコッチのうち場にも手札にもいないものを、"
            "それぞれ1枚ずつ優先してベンチに出す。両方すでに場にいる場合は、"
            "ケーシィの2枚目以降、またはノコッチの2枚目を優先する。"
        ),
    ),
    UsageNote(
        card_id=1097,
        name="夜のタンカ",
        note=(
            "基本（たね）ポケモンを優先して回収する。回収基準は OPENING_BENCH_PRIORITY"
            "の並びに準拠しつつ、場・手札に進化先がいないポケモンの回収を優先する。"
        ),
    ),
    UsageNote(
        card_id=1129,
        name="せいなるはい",
        note="トラッシュにポケモンが3体以上ある場合に使用する。",
    ),
    UsageNote(
        card_id=ENERGY_RECYCLE_TOOL_CARD_ID,
        name="ワンダーパッチ",
        note="トラッシュに基本【超】エネルギーがある場合に使用する。",
    ),
    UsageNote(
        card_id=1152,
        name="ポケパッド",
        note="手札にあれば基本的に使用する。",
    ),
]

SUPPORTER_USAGE_NOTES: list[UsageNote] = [
    UsageNote(
        card_id=1225,
        name="トウコ",
        note=(
            "手札にあれば基本的に使用する。進化ポケモンとエネルギーを1枚ずつサーチできる。"
            "エネルギーは基本的にリッチエネルギーを回収する（SEARCH_PRIORITY参照）。"
            "進化ポケモンの対象は SEARCH_PRIORITY_RULES に従い、ノコッチが場にいる／"
            "ポケパッド・なかよしポフィンで持ってこれる場合はノココッチを、"
            "そうでなければユンゲラー系列を優先する。"
        ),
    ),
    UsageNote(
        card_id=1231,
        name="ヒカリ",
        note="手札にあれば基本的に使用する。サーチ対象は OPENING_BENCH_PRIORITY の並びに準拠する。",
    ),
    UsageNote(
        card_id=1182,
        name="ボスの指令",
        note="相手のベンチポケモンのHPが、相手のバトルポケモンのHPより低い場合に使用する。",
    ),
    UsageNote(
        card_id=1184,
        name="スイレンのお世話",
        note=(
            "「トラッシュにエネルギーがあり、バトル場のポケモンのエネルギーが足りず、"
            "手札にもエネルギーがない」場合、または"
            "「トラッシュにフーディン・ユンゲラー・ケーシィの3体がそろっている」場合に使用する。"
        ),
    ),
]

STADIUM_USAGE_NOTES: list[UsageNote] = [
    UsageNote(
        card_id=1264,
        name="バトルコロシアム",
        note="場のスタジアムがバトルコロシアム以外なら、手札にあれば出す。",
    ),
]


# ---------------------------------------------------------------------------
# きぜつ後の後継優先順位（バトル場の入れ替え）
# ---------------------------------------------------------------------------

KO_REPLACEMENT_PRIORITY: list[PriorityEntry] = [
    PriorityEntry(card_id=743, name="フーディン", reason="立て直しの主砲。最優先で後継に。"),
    PriorityEntry(card_id=742, name="ユンゲラー", reason="フーディンまで進化1段のため次点。"),
    PriorityEntry(card_id=741, name="ケーシィ", reason="フーディン系列の最初期。"),
    PriorityEntry(card_id=65, name="ノコッチ", reason="ドローエンジンの前段。他に候補がない場合の後継。"),
    PriorityEntry(card_id=66, name="ノココッチ", reason="本来はベンチ固定だが、他に候補がない場合の最終手段。"),
    PriorityEntry(card_id=343, name="シェイミ", reason="攻撃力が低いため最終手段。"),
]


# ---------------------------------------------------------------------------
# 全体メモ
# ---------------------------------------------------------------------------
#
# ・サポート／グッズのドロー効果はポケモンかエネルギーのみが対象なので、
#   回収・サーチ先の優先順位は OPENING_BENCH_PRIORITY / ENERGY_TARGET_PRIORITY に準拠する。
# ・山札の残り枚数を意識し、手札が一定以上あるときは山札からドローする
#   グッズ・サポートを追加では使わない（デッキアウト対策、既に手札は足りているため）。

DECK_DRAW_ITEM_STOP_HAND_SIZE = 20


# ---------------------------------------------------------------------------
# サイド枚数ごとの勝ち筋・立て直し方
# ---------------------------------------------------------------------------

WIN_CONDITIONS_BY_PRIZE: list[PrizeStageWinCondition] = [
    PrizeStageWinCondition(
        prize_range="6〜4枚（序盤）",
        plan=(
            "ケーシィ→フーディンの進化ルートを確立しつつ、ノコッチ→ノココッチの"
            "にげあしドローで手札を積む。シェイミを出せたらベンチの展開札を"
            "相手のワザダメージから守る。"
        ),
    ),
    PrizeStageWinCondition(
        prize_range="3〜2枚（中盤）",
        plan=(
            "フーディンのハンドパワーを継続して打ち、手札枚数を維持しながら"
            "大ダメージを通す。相手の重要な後続はボスの指令＋ハンドパワーで刈り取る。"
        ),
    ),
    PrizeStageWinCondition(
        prize_range="1枚（詰め）",
        plan="相手の残りポケモンのHPを見て、ハンドパワーで正確に処理する。",
    ),
    # 元々「自分のポケモンが取られた直後」の立て直しはキチキギスexの特性さかてにとる
    # （3ドロー）が担っていたが、デッキから抜けたため現在この場面の専用プランは無い。
    # 1197（ゼロシックの策略）を含め、代替の立て直し方はまだ検討していない。
]


# ---------------------------------------------------------------------------
# 対アーキタイプ戦略（相手デッキ予測との連携）
# ---------------------------------------------------------------------------
#
# opponent_modeling.rough_predictor が「相手はこのアーキタイプらしい」と確信を持てた
# （status == "confident"）場合にだけ参照される、技/カードの優先度加点。
# キーは rough_predictor.json の archetypes キーと一致させること。
#
# card_priority_boost はスコア10前後の OPENING_BENCH_PRIORITY クラスタ（ケーシィ〜シェイミの
# 7枚、差は0.01刻み）の中での順位を入れ替える程度の大きさ（+-1.5〜3.0）にしてある。
# 大きくしすぎると評価値20の進化行動より優先されてしまうため、あくまで
# 「このクラスタ内での優先変更」に留まる範囲で加点している。
# 改造ハンマー(1081)は ItemProfile.priority=0.5（グッズは0.0〜1.0のスケール）なので、
# 同スケールに合わせて+0.4程度の加点にしている。
#
# シェイミは特性はなのカーテン（ベンチへのダメージ無効化）が唯一の役割で、攻撃力
# （スマッシュキック30）は低い。ベンチ狙撃技を持たない相手には「出しても腐る」1枠に
# なるため、ベンチ狙撃が無いと確認できたアーキタイプにはマイナス加点（-3.0）を入れ、
# シェイミより他の展開（進化ルート等）を優先させる。減点しても、他に合法な選択肢が
# 無いターンではシェイミが選ばれる（board.py は毎ターンの合法候補の中からしか選ばない）。
#
# attack_priority_boost は「現在バトル場にいる1体が持つ複数ワザ」の中でしか選ばれない
# （cg/api.py: attack.py は player.active[0] の技だけを見る）。このデッキで技を2つ以上
# 持つのはノコッチ(65: かじる/ほる)だけで、フーディン・シェイミなどは
# 技が1つしかないため、「相手がこのアーキタイプなら別の攻撃役に切り替える」判断は
# retreat.py 側（バトル場に誰を出すか）の役目であり、attack_priority_boost では
# 実現できない。そのためここでは attack_priority_boost の使用は見送っている。
#
# 各エントリの根拠は cg.api.all_card_data()/all_attack() で実際のワザ・特性テキストを
# 確認して判断した（date取得時点。カードデータはコンペ提供の cg エンジン内蔵データで、
# data/*_Card_Data.csv とは別に実行時取得できる）。
MATCHUP_PLANS: dict[str, MatchupPlan] = {
    "dragapult_ex": MatchupPlan(
        card_priority_boost={343: 3.0},  # シェイミ
        note=(
            "ドラパルトexの「ファントムダイブ」はダメカン6個（=60ダメージ）を相手の"
            "ベンチに自由配分できるベンチ狙撃技。シェイミの特性はなのカーテンは"
            "ルール（ex/V等）を持たないベンチポケモンへのダメージを完全無効化するため、"
            "確信を持てた時点でシェイミの展開を優先する。"
        ),
    ),
    "gekkouga_ex": MatchupPlan(
        card_priority_boost={343: 3.0},  # シェイミ
        note=(
            "ゲッコウガexの「幻影らんげき」は相手のポケモン2体（ベンチ含む、弱点/抵抗力"
            "無視）に120ダメージを与えるため、ドラパルトex以上にベンチ全体への脅威が大きい。"
            "シェイミのはなのカーテンで無効化できるので優先して場に出す。"
        ),
    ),
    "oliva_ex": MatchupPlan(
        card_priority_boost={343: 3.0},  # シェイミ
        note=(
            "オリーヴァexの「オイルサルボ」は相手の場のポケモンを合計6回選んで"
            "1回20ダメージ（弱点/抵抗力無視）を割り振れる技で、ベンチ複数体を"
            "同時に削られうる。シェイミのはなのカーテンで無効化できるため優先する。"
        ),
    ),
    "mega_starmie_ex": MatchupPlan(
        card_priority_boost={343: 1.5},  # シェイミ（主砲に添える程度の付随ベンチ狙撃）
        note=(
            "メガスターミーexの「ジェットブロー」は主目的（120ダメージ）に加えて"
            "ベンチに50ダメージが付随する程度で、上記3アーキタイプほど深刻ではないため"
            "加点は控えめにする。"
        ),
    ),
    "marnie_grimmsnarl_ex": MatchupPlan(
        card_priority_boost={343: 1.5},  # シェイミ（30ダメージの付随ベンチ狙撃のみ）
        note="マリィのオーロンゲexの「シャドウバレット」はベンチへの付随ダメージが30と小さいため軽めの加点にする。",
    ),
    "mega_lucario_ex": MatchupPlan(
        card_priority_boost={1081: 0.4, 343: -3.0},  # 改造ハンマー / シェイミ
        note=(
            "メガルカリオexは基本ではない「ロック【闘】エネルギー」（特殊エネルギー）を"
            "前提にした構築。改造ハンマー（相手の特殊エネルギーを破壊するグッズ、"
            "通常優先度0.5）を通常のグッズ優先度より少し上げ、エネルギー加速を崩しにいく。"
            "「メガブレイブ」270ダメージは単体特化でベンチ狙撃技を持たないため、"
            "シェイミは温存してよい（他の展開を優先）。"
        ),
    ),
    "alakazam": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note=(
            "このアーキタイプはフーディン系列＋シェイミ＋改造ハンマーなど本デッキと"
            "近い構築（ミラー、または似た構築のフーディン系デッキ）を指す。"
            "「サイコウェーブ」「ふしぎハッキング」共にベンチ狙撃技ではないため、"
            "ミラー戦ではベンチ防御より進化ルートの構築速度を優先する。"
        ),
    ),
    "kamitsuorochi_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note="カミツオロチexの「シロップストーム」は自分のエネルギー数依存の単体攻撃で、ベンチ狙撃技を持たない。",
    ),
    "takeruraiko_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note=(
            "タケルライコexの「ワイルドボルト」はエネルギーを捨てて火力を伸ばす単体攻撃、"
            "「バーストロア」は手札入れ替え。どちらもベンチ狙撃技ではない。"
        ),
    ),
    "ogerpon_teal_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note="オーガポンみどりのめんexの「みだれ葉シャワー」は両者の場のエネルギー数依存の単体攻撃で、ベンチ狙撃技を持たない。",
    ),
    "shirona_garchomp_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note=(
            "シロナのガブリアスexの「ドラゴンバスター」（260、自身のエネルギー全て捨てる）"
            "「コークスクリューダイブ」（ドロー）はどちらも単体攻撃で、ベンチ狙撃技を持たない。"
        ),
    ),
    "toxtricity": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note="ストリンダーの「ジェントルスラップ」は単体100ダメージで、ベンチ狙撃技を持たない。",
    ),
    "rocket_honchkrow": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note="ロケット団のドンカラスの技はどちらも手札のサポートを捨てて火力を伸ばす単体攻撃で、ベンチ狙撃技を持たない。",
    ),
    "crustle": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note=(
            "イワパレスは単体攻撃のみでベンチ狙撃技を持たない。特性で相手の"
            "ex/メガポケモンからのダメージを自身が無効化する低速デッキのため、"
            "こちらもベンチ防御より押し切りを優先する。"
        ),
    ),
    "mega_froslass_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note=(
            "メガユキメノコexの「リセントフルリフレイン」は相手（＝こちら）の手札枚数"
            "依存の単体攻撃で、ベンチ狙撃技ではない。むしろ本デッキが手札を溜めるほど"
            "被弾が大きくなる相性なので、ベンチ展開より打点確保を優先すべき相手。"
        ),
    ),
    "mega_abomasnow_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note="メガユキノオーexの「ハンマーアバランチ」は自分の山札を掘って火力を伸ばす単体攻撃で、ベンチ狙撃技を持たない。",
    ),
    "archaludon_ex": MatchupPlan(
        card_priority_boost={343: -3.0},  # シェイミ
        note="ブリジュラスexの「メタルディフェンダー」は単体攻撃＋次ターン弱点無効化で、ベンチ狙撃技を持たない。",
    ),
}

