# 新デッキ 固定シナリオ 下書き（⑨・担当A）

## 位置づけ

new-deck-src-structure.md の⑨（検証）のうち、担当Aが下書きする「このデッキの固定シナリオ
（どの盤面で、何を選ぶべきか）」。まだ pytest コードとしては実装していない。

- ③ メイン行動の意思決定（`ptcg_ai/rule_based/main_turn_parts/` 等）は、この
  `feature/new-deck-inada` ブランチにはまだ存在しない。`origin/feature/rule-based`
  （コミット `d988f33`）に①〜⑤の実装と、Bが用意した汎用テスト雛形
  （`tests/conftest.py` / `tests/helpers_decision.py` /
  `tests/unit/test_router_dispatch.py` など）がすでにある。
- ここに書くシナリオは、そのブランチがマージされた後に
  `tests/decks/new_deck/test_ability_priority.py` 等へ落とし込み、担当Bとすり合わせる想定。
- 参照元： [deck_plan.py](sample_submission/ptcg_ai/decks/new_deck/deck_plan.py) /
  [pokemon_profiles.py](sample_submission/ptcg_ai/decks/new_deck/pokemon_profiles.py) /
  [attack_profiles.py](sample_submission/ptcg_ai/decks/new_deck/attack_profiles.py)

各ポケモンの実データ（`cg.api.all_card_data()` / `all_attack()` で確認済み）:

| card_id | 名前 | HP | 弱点 | にげるコスト | 主なワザ(attackId) |
|---|---|---|---|---|---|
| 741 | ケーシィ | 50 | 悪 | 1 | テレポートアタック(1070) 超1・10dmg |
| 742 | ユンゲラー | 80 | 悪 | 1 | ちょうねんりき(1071) 超1・30dmg |
| 743 | フーディン | 140 | 悪 | 1 | ハンドパワー(1072) 超1・手札×2dmg |
| 65 | ノコッチ | 60 | 闘 | 0 | かじる(74)無1・10dmg／あなをほる(75)無2・30dmg |
| 66 | ノココッチ | 140 | 闘 | 3 | ランドクラッシュ(76) 無3・90dmg |
| 140 | キチキギスex | 210 | 闘 | 1 | クルーエルアロー(183) 無3・100dmg(単体・ベンチ可) |
| 343 | シェイミ | 80 | 炎 | 1 | けとばす(477) 無2・30dmg |

---

## 1. 特性優先順位（test_ability_priority.py 相当）

### 1-1. ユンゲラー／フーディンへの手札進化直後
- **盤面**: 手札のユンゲラー(742)を出してケーシィ(741)から進化させた直後。特性サイコドロー使用可。
- **期待する選択**: 基本的に使用する（2ドロー）。手札が `DECK_DRAW_ITEM_STOP_HAND_SIZE`(20枚)を
  超えている場合のみ見送りを検討。
- **フーディン(743)版**: 同様にサイコドロー(3ドロー)を基本的に使用する。

### 1-2. ノココッチ「にげあしドロー」の使用判断
- **盤面**: ベンチのノコッチ(65)がノココッチ(66)に進化済み、今ターンまだ特性未使用。
- **期待する選択**: 基本的に使用する（3ドロー後、自身と付属カードは山札へ）。
- **注意点**: 使用するとベンチの1枠（`RESERVED_BENCH_SLOTS_FOR_DRAW_ENGINE`対象）が空くため、
  展開したい他のポケモンが手札にない場合は空いた枠がそのまま無駄になる。ベンチが埋まっていて
  展開の余地がない時にどう扱うかは③実装時にBとすり合わせる。

### 1-3. キチキギスex「さかてにとる」の使用判断
- **盤面A**: 前の相手の番に自分のポケモンがきぜつした。今ターンまだ同アビリティ未使用。
- **期待する選択**: 使用する（3ドロー）。
- **盤面B**: 前の相手の番に自分のポケモンがきぜつしていない。
- **期待する選択**: 使用不可（選択肢に出ないはず）。ロジック側でこの前提条件を正しく判定できるかの確認ポイント。

### 1-4. シェイミ「はなのカーテン」
- **盤面**: シェイミ(343)が場にいる。
- **期待する選択**: 常時効果のため「使うかどうか」を選ぶ場面自体が発生しない。
  選択肢生成側がこれを能動的スキルとして提示しないことの確認シナリオ。

---

## 2. 攻撃優先順位（test_attack_priority.py 相当）

### 2-1. ハンドパワーで打点が足りる場合
- **盤面**: フーディン(743)がバトル場、超エネルギー1個付与済み。手札8枚
  （ダメージ16）。相手バトルポケモンの残りHPが16以下。
- **期待する選択**: ハンドパワーで攻撃してKOを取る。

### 2-2. ハンドパワーで打点が足りない場合
- **盤面**: 同上だが相手バトルポケモンの残りHPが16を大きく上回る。
- **期待する選択**: 深追いで攻撃せず、まず手札を伸ばす行動（ドロー系グッズ／サポート）を優先し、
  次のターン以降により大きいハンドパワーを狙う（deck_planの基本方針「できるだけ手札を多くして
  フーディンで攻撃する」に整合）。

### 2-3. クルーエルアローでのベンチ狙撃
- **盤面**: キチキギスex(140)がバトル場、無エネルギー3個付与済み。相手ベンチに
  低HPの重要な後続候補がいる。
- **期待する選択**: クルーエルアロー（100dmg・ベンチ選択可）でその後続を狙撃する。
  `attack_profiles.PROFILES[183].bench_snipe == True` の裏付けとして使うシナリオ。

### 2-4. ノコッチ／ノココッチがやむを得ずバトル場にいる場合
- **盤面**: 何らかの理由でノコッチ(65)またはノココッチ(66)がバトル場にいる
  （本来は `BENCH_ONLY_SUPPORT` によりベンチ固定方針）。
- **期待する選択**: この状況自体がイレギュラーである前提を確認するシナリオ。
  発生した場合はノコッチは「あなをほる」でエネルギーが足りれば次ターンの被弾を軽減しつつ粘る、
  ノココッチは即座に撤退候補にする（1-2, 3-2 のシナリオ参照）。

---

## 3. デッキプロファイル検証（test_deck_profiles.py 相当）

盤面シナリオではなく、静的データの整合性チェック。⑤の `profile_registry` 経由で
参照できるようになった後に実装する。

- `deck.csv` の全カードID（22種、60枚）が `pokemon_profiles` / `item_profiles` /
  `supporter_profiles` / `tool_profiles` / `stadium_profiles` / `energy_profiles`
  のいずれかに存在する（本ブランチで作成時にスクリプトで確認済み、回帰テスト化する）。
- `attack_profiles.PROFILES` のキーが `cg.api.all_attack()` から取得した実際の
  attackId（74, 75, 76, 183, 477, 1070, 1071, 1072）と一致する。
- `ENERGY_REQUIRED_COUNT`（deck_plan.py）の値が、実際のワザコスト
  （`cg.api.all_attack()` の `energies` の長さ）と矛盾しない
  （例: 66→3、140→3、65→2、743/742/741→1、343→2）。

---

## 4. メインターン統合（test_main_turn_integration.py 相当）

### 4-1. 序盤の展開順（サイド6枚）
- **盤面**: 初手。手札にケーシィ(741)・ノコッチ(65)。ベンチ空。
- **期待する選択**: `OPENING_BENCH_PRIORITY` 通り、ケーシィを優先して展開し、
  ノコッチもベンチに出す。

### 4-2. ドロー後に攻撃するか、攻撃を先にするか
- **盤面**: バトル場フーディン(743・超1個)、手札12枚。ベンチにノココッチ(66・特性未使用)。
- **期待する選択**: このターン中ににげあしドローを使ってから攻撃するか、攻撃を先に行うかは
  結果に差が出ない場合が多い（ハンドパワーのダメージ判定は攻撃宣言時点の手札枚数に依存するため、
  攻撃前にドローを済ませて手札を増やしてから攻撃する方が高ダメージになる）。
  → **ドロー系の行動を先に済ませてから攻撃する**、の順序を明文化する。

### 4-3. 除去（ボスの指令＋ハンドパワー）のコンボ
- **盤面**: 相手ベンチに残りHPが低い進化前ポケモンがいる。手札にボスの指令(1182)。
  フーディンが攻撃可能。
- **期待する選択**: ボスの指令で対象をバトル場に引きずり出してから、同ターンにハンドパワーで
  KOを狙う（サポート→攻撃の順序）。

---

## 5. 撤退優先順位（test_retreat_priority.py 相当）

### 5-1. フーディンが次の相手の番にきぜつしそうな場合
- **盤面**: バトル場フーディン(743・HP140・残りHP少)、ベンチにユンゲラー(742)。
  にげるコスト1がまかなえる。
- **期待する選択**: 手札を維持できる状況なら、ハンドパワーを一度でも多く通した方が得な場合が
  多いため、無理に撤退せず攻撃を優先するケースと、次のターンの反撃で確実に取られる場合は
  撤退してユンゲラーに繋ぐケースを分けて検討する（`KO_REPLACEMENT_PRIORITY` と対になる
  「まだ倒される前に自分から逃げる」判断）。

### 5-2. ノココッチがバトル場に出てしまった場合
- **盤面**: 何らかの理由でノココッチ(66)がバトル場にいる。にげるコスト3。
- **期待する選択**: `BENCH_ONLY_SUPPORT` の方針に従い、にげるコスト3を払ってでも
  最優先でベンチに戻す。ただしにげるコストが重いため、ワンダーパッチ等でのエネルギー確保状況次第では
  即座に払えないことがある点に注意（払えない場合は次ターン以降に持ち越し）。

### 5-3. シェイミがバトル場に出てしまった場合
- **盤面**: シェイミ(343)がバトル場にいる。にげるコスト1。
- **期待する選択**: 低コストで撤退可能なので、次の自分の番に最優先でベンチへ戻す
  （ベンチ保護の役割に戻す）。

### 5-4. ノコッチ（進化前）がバトル場にいる場合
- **盤面**: ノコッチ(65)がバトル場。にげるコスト0。
- **期待する選択**: 無償で撤退できるため、ベンチ運用に戻すコストはほぼゼロ。
  優先的に撤退させて良い。
