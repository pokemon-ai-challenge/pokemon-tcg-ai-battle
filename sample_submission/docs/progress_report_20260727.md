# 進捗報告(2026/07/27)

## 本日やったこと

**ベイズ推定を使用してにげるの判定用に実装**

1. **`feature/bayesian-agent`ブランチを新規作成**

`integration`から新規作成し、ベイズ推定の適用先として「相手が次ターンKOしてくる確率」を「にげる」判断に使う方向で実装。

2. **`likely_ko_probability_next_turn()`を追加（`board_features.py`）**

既存の相手デッキ推定（`OpponentHiddenState.marginals()`）を使い、「あと1エネルギーで足りる」ケースを確率化。`retreat.py`のトリガーAをconfig駆動（`probabilistic_ko.enabled`/`threshold`）で切り替え可能に。単体テストも追加。

3. **Kaggle上位陣のリプレイでバックテスト実施**

`pip install kaggle`等の環境構築から始め、1,002件のリプレイを取得。専用の検証スクリプト（`evaluate_retreat_ko_probability.py`）を新規作成し、13,526サンプルで検証。この過程で`read_deck_csv()`の相対パス依存によるサイレントな不具合を発見・修正。

4. **検証結果を受けて閾値・有効化を変更**

閾値を0.5→0.2に変更し、`enabled`をtrueに切り替え（実運用で有効化）。既存テストが実configの値に依存しないよう修正。

5. **バトルビューアーに「にげる判断」デバッグタブを新規追加**

実際の対局でKO確率・旧ブール判定・トリガーA判定結果が正しく表示されることをブラウザで確認。

## 昨日の提出結果から考えたこと

1. 手札最大化を行うと不思議なアメを使用しなくなり、ケーシィ・フーディン・エネがあるのに負けた。

## 次に行いたいこと

1. ベイズ推定を別のところにも適用
2. 手札最大化改善
